CREATE OR REPLACE FUNCTION public.__import_exec(sql text) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, extensions, auth AS $$ BEGIN EXECUTE sql; END; $$;
REVOKE ALL ON FUNCTION public.__import_exec(text) FROM PUBLIC, anon, authenticated;